

let subStr = "";


//No funciona, no tiempo para dedicarle exclusivo. Arreglar tras la entrega
export function getSubStr(string) {
    for (let letra = 0; letra <= 50; letra++) {
        subStr = string[letra];
    }
    return subStr
}